# Deferred Post-Restoration Work

## Reverse round 2 — Fix A item visual parity

Ground truth: `geometry_wars_3d_glm5_2.html.bak_reverse_base`, SHA-256
`4dbb6b707725b9b01314bd9eaec4c7a3c6ec7ccfcff1d79fa383f3df9f3ebfbf`.

| Item render surface | Original value | Before Fix A | After Fix A |
|---|---|---|---|
| Core geometry | Octahedron radius `0.5`, detail `0` | Same | Same |
| Core material | additive `MeshBasicMaterial`, `depthWrite=false`, opaque defaults | Equivalent shared helper added explicit opacity/transparent fields | Direct original material options |
| Ring geometry | open cylinder `0.72/0.72/0.08`, 16 segments | Same | Same |
| Ring material | white wireframe, additive, transparent, opacity `0.72`, no depth write | Same | Same |
| Float/pulse | `sin(bob)*0.4+0.6`; Y `0.5+bob*0.4`; scale `0.7+bob*0.25` | Same | Same |
| Rotation | core `(time*1.5+bob, time*1.1, 0)`; ring `(PI/2, time*1.6+bob, 0)` | Same | Same |
| Removed instance cleanup | move stale slots to Y `-9999`, zero scale, retain high-water count | Reduced draw count directly | Original high-water/hide behavior restored |
| Lazy instance-color shader | Original bundle shows the core on first colored item | Modular build reused the pre-color opaque program and omitted the core | Recompile the core material when `instanceColor` is first allocated |
| Label/color fallback | kind uppercase and white fallback | Relied on strict runtime types | Original fallbacks restored |
| Magnet motion | radius `11`, pull `36`, quadratic near-field pull | Same | Same |

Evidence is generated by `node scripts/visual-parity.mjs` in
`.tmp/reverse/visual-parity/`; the script freezes both games to the same world
state and camera before taking the original/restored pair.

These items were intentionally kept outside the parity restoration:

## Reverse round 2 — Phase C BGM layer

| Surface | Result |
|---|---|
| File tracks | `src/audio/bgm.ts`, base/boss looped HTMLAudio tracks |
| Crossfade | 1.8s base↔boss fade; game-over fade-out; restart resets base |
| Fallback | Load/play failure calls `AudioEngine.setExternalBgmActive(false)`; synth BGM remains audible |
| Mix | External base 0.5, boss 0.6; SFX bus is never muted |
| Mute/autoplay | Shared mute state; playback begins only from `beginGame()` user gesture |
| Single build | `MODE=single` skips external track loading; synth-only fallback |

Validation: `npm run typecheck`, `npm test` (143 tests), `npm run parity:verify` (3/3),
`npm run build`, and `npm run build:single` all pass. Ground-truth SHA remains
`4DBB6B707725B9B01314BD9EAEC4C7A3C6EC7CCFCFF1D79FA383F3DF9F3EBFBF`.

## Addendum — audio finalization

- Confirmed tracks are wired to `assets/bgm/base.ogg` and `assets/bgm/boss.mp3`.
- Added `assets/sfx/lightning.ogg` one-shot playback only for lightning fire.
- Lightning sample uses world pan, gain `0.55`, 75ms minimum interval, and a
  four-voice cap with oldest-voice cutoff. Decode/fetch failure retains the
  original procedural lightning SFX.
- Other SFX paths are unchanged; simulation remains audio-independent.

- Add an approved staging Supabase project before automating leaderboard write tests. The current smoke performs no production write.
- Consider screenshot-diff and captured-audio regression baselines. Current automation proves nonblank rendering, layout bounds, interaction flow, and simulation identity, but not pixel- or waveform-identical output.
- Upgrade the Vite 5 / Vitest 1 development toolchain in a separate change. `npm audit --omit=dev` is clean; development-only audit findings should not be mixed into the restored gameplay baseline.
- Review render-only boss choreography with the canonical HTML during Jun's final visual pass, then treat any approved visual adjustment as a new baseline rather than restoration work.
