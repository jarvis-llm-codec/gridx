# Codex 편지 — ① 아이템을 리버스 전 모습(큰 구형)으로 ② 사망 대폭발음 배선

## 배경 (Jun 판정)
1. **아이템**: "리버스하기 전이 더 보기 좋았어. 좀 커다란 구형이어서." — 즉 현재 복원본 아이템이 원본(정본 HTML)과 아직 다르다. Fix A의 verbatim 이식이 불완전했을 가능성 (visual-parity 산출물에 `restored-core-debug.png`, `original-items-core-only.png` 같은 부분 비교 흔적 있음 — 전체가 아니라 core만 맞췄나 확인하라).
2. **사망 폭발음**: 보스 사망 + 주인공 사망 시 큰 폭탄 소리. 파일 준비됨.

## 작업 1 — 아이템 시각 재수리 (ground truth = 정본 HTML)
1. 정본 `geometry_wars_3d_glm5_2.html.bak_reverse_base`에서 픽업/아이템 메시 생성 코드를 다시 찾아 **지오메트리 종류·반경·스케일·머티리얼 전부** 현재 구현과 숫자 단위로 대조 (diff 표 작성).
2. Jun 증언 반영: 원본은 **"커다란 구형(sphere)"**. 현재 렌더가 구형이 아니거나 작다면 그게 어긋난 지점이다.
3. 원본 값으로 교체. 만에 하나 원본을 정확히 재현했는데도 Jun이 더 크길 원하면을 대비해 **시각 스케일 계수를 `CONFIG.itemVisual.scale`로 노출** (기본 1.0 = 원본).
4. ⚠️ `CONFIG.items.radius`(0.5)는 심 판정용 — **절대 변경 금지** (패리티 깨짐). 시각 크기만 별도로.
5. 검증: 같은 시드 나란히 스크린샷 갱신 + 골든 패리티 diff 0 + Jun 눈.

## 작업 2 — 사망 대폭발음
- 파일: `assets/sfx/death.ogg` (3.2s, 저음 대폭발, CC0 — 배치 완료).
- 배선: **보스 사망**(boss.ts `eventTier:'death'`)과 **주인공 사망/게임오버**(player.ts `game-over` 이벤트) 시 재생. 기존 샘플 맵에 `death` 슬롯 추가 (원샷, 권장 볼륨 0.8, 스로틀 불필요 — 드문 이벤트).
- 주인공 사망 시 기존 `playGameOver()` 신스와 겹치면: death.ogg를 주로, 신스 게임오버는 유지하되 볼륨 낮춰 공존 (귀 판정은 Jun).
- 폴백: 파일 실패 시 기존 신스 경로.

## 규칙 (동일)
시뮬 불변·패리티 diff 0·테스트 그린·타입체크·정본 SHA 불변. **단독 커밋까지 완료 후 보고.**
