# Codex 편지 — 역복원 라운드 2: 시각·청각 패리티 수리 + BGM 업그레이드 준비

## 0. 배경
라운드 1(ef7f784) 왕 GAN 판정: 시뮬 패리티 PASS. 그러나 **Jun 최종 눈·귀 검수에서 리그레션 2건 발견**:
1. **아이템(픽업) 생김새가 원본과 다름** — 원본이 더 예뻤다 (Jun 확언)
2. **음악이 원본과 다름** — 기본 BGM·보스 등장 음악이 바뀜

원인 진단: 시뮬은 골든 궤적(1e-9)이 심판이었지만 렌더·오디오는 기계 심판이 없었다. 재해석하지 말고 **원본 코드를 축자(verbatim) 이식**하는 것이 이번 라운드의 규칙이다.

## 1. Ground Truth
- 원본: `geometry_wars_3d_glm5_2.html.bak_reverse_base` (SHA `4dbb6b…bfbf`, 읽기 전용)
- 라운드 1에서 추출한 원본 게임코드가 `.tmp/reverse/`에 있으면 재활용, 없으면 재추출.

## 2. Fix A — 아이템 시각 패리티 (재해석 금지, 축자 이식)
1. 원본 게임코드에서 **픽업/아이템 관련 렌더 전부** 찾기: 지오메트리 생성(형태·크기·세그먼트), 머티리얼(색·emissive·투명도·블렌딩), 애니메이션(회전·wobble·부유·펄스), 라벨/글로우, 자석 흡인 연출.
   - 힌트: 라운드 0 이전 프로브 `.tmp/geometry_wars_3d_glm5_2_item_labels*.diff`, `item_label_probe.mjs`, `item_magnet_probe.mjs` — 아이템 코드 위치 지도가 이미 있다.
2. 현재 `src/render/`(entityRenderer 등)의 해당 부분과 **코드 레벨 diff 작성** → 어긋난 모든 지점 목록화 (`RESTORE_NOTES.md`에 표로).
3. 원본 쪽으로 전부 교체. **"더 낫다" 판단 금지** — 원본과 같으면 정답, 다르면 오답.
4. 검증: ①diff 표의 모든 항목 해소 ②스크린샷 비교 — 원본 HTML과 복원 빌드를 같은 시드로 띄워 아이템 드랍 시점 캡처 나란히 저장 (`.tmp/reverse/visual-parity/`) ③최종 판정은 Jun 눈.

## 3. Fix B — 오디오 엔진 패리티 (신스 파라미터 축자 이식)
1. 원본의 오디오 클래스(최소화명 `Mg`) 전체를 추출해 현재 `src/audio/AudioEngine.ts`와 대조.
2. 대조 대상 전부: 오실레이터 타입/주파수/디튠, 엔벨로프(ADSR), 필터, 노이즈 소스, 시퀀서 패턴(기본 BGM 루프), **setBossMode 전환 연출**, playEvents의 SFX 매핑(발사·명중·폭발·픽업·멀티플라이어·NOVA), 팬닝(플레이어 x좌표 기반), 마스터 게인.
3. 어긋난 파라미터 전부 원본값으로. 역시 재해석 금지.
4. 검증: ①파라미터 diff 표 (원본값 vs 수정 전 vs 수정 후) ②오프라인 렌더 비교 — OfflineAudioContext로 같은 이벤트 시퀀스를 원본/복원 각각 렌더 → 파형 RMS/스펙트럼 유사도 리포트 (`.tmp/reverse/audio-parity/`) — 완벽 일치까진 요구 안 하나 명백히 같은 소리여야 함 ③최종 판정은 Jun 귀.

## 4. Phase C — 진짜 BGM 레이어 준비 (구현하되, 트랙 파일은 대기)
패리티(A·B) 완료 후에만 착수. Jun이 원하는 것: **기본 BGM + 보스 등장 BGM을 "제대로 된 음악"으로**.

1. **BGM 매니저 구현** (`src/audio/bgm.ts`):
   - 인터페이스: `loadBgm({ base: url, boss: url })`, 상태머신 base↔boss **크로스페이드**(1.5~2s), 게임오버 시 페이드아웃, restart 시 base 재시작.
   - 기존 신스 엔진과 공존: BGM 트랙이 로드되면 신스의 음악 레이어만 음소거하고 SFX는 유지. 트랙이 없거나 로드 실패 시 **신스 BGM으로 자동 폴백** (오프라인/파일 누락에도 무음 금지).
   - 뮤트 버튼(M)이 BGM+SFX 동시 제어. 모바일 자동재생 정책 준수 — 재생 시작은 반드시 사용자 제스처(GAME START 클릭) 이후.
   - 볼륨: BGM 기본 0.5, 보스 전환 시 0.6, config에 노출.
2. **파일 전략**: `assets/bgm/base.mp3`, `assets/bgm/boss.mp3` 경로로 로드 (멀티파일 배포 전제). `build:single`에서는 BGM **제외**(신스 폴백 사용)를 기본으로 하되, `--inline-bgm` 옵션으로 base64 인라인도 가능하게 (파일당 +33% 부풀음 경고 로그).
3. **트랙 파일 자체는 이 편지 범위 밖** — Jun이 제공 예정(AI 생성 또는 CC0). 더미 무음 mp3로 로드·크로스페이드·폴백 경로만 테스트해 둘 것.
4. 검증: 더미 트랙으로 base→boss→base 전환, 뮤트, 폴백, 모바일 제스처 시나리오 각 1회 + 테스트 코드.

## 5. 하우스룰 (라운드 1과 동일 + 강화)
- 정본/백업 HTML 무수정 (작업 후 SHA 재확인해 보고에 포함).
- 시뮬 패리티 회귀 금지: A·B·C 끝날 때마다 골든 궤적 재실행 — **diff 0 유지**.
- vitest 전체 통과 유지. 커밋은 Fix A / Fix B / Phase C 단위.
- 배포·Supabase·gh-pages 접근 금지.
- 상태 보고: DONE(증거)/BLOCKED(원인+다음 액션)만.

## 6. 완료 기준
1. Fix A: 아이템 diff 표 전항목 해소 + 나란히 스크린샷 + Jun 눈 승인
2. Fix B: 파라미터 diff 표 + 오디오 유사도 리포트 + Jun 귀 승인
3. Phase C: 더미 트랙으로 전 시나리오 통과, 트랙 꽂으면 바로 켜지는 상태
4. 골든 시뮬 패리티 diff 0 유지, 141+ 테스트 통과, 정본 SHA 불변

---
작업 폴더: `C:\jarvis_workspace\geometry_wars_3d_v2\` (git, HEAD=ef7f784)
